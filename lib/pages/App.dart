import 'package:flutter/material.dart';
import 'BottomNavigation.dart';

class App extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: (BattomNavigation()),
    );
  }
}
